const Auth = require("../../main/express/server/Auth");

class {{ MiddlewareName }} {

    static handle(next) {
        // your middleware logic here
        // const checker = Auth.check();
        // if (!checker){
        //     return jsonResponse({ message: "Unauthorized" }, 401);
        // }
        next();
    }
}

module.exports = {{ MiddlewareName }};